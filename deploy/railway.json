{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "../Dockerfile"
  },
  "deploy": {
    "startCommand": "claide gateway",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 5,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
